{{- define "initDefaults" -}}
func ({{.Recv}} *{{.Struct}}) InitDefaults() {
{{- range .Parents }}
	{{$.Recv}}.{{.}}.InitDefaults()
{{- end }}
{{- range .Fields }}
{{- if and .Default (not (isEmptyDefault .Default)) }}
	{{$.Recv}}.{{.FieldName}} = {{.Default}}
{{- end }}
{{- end }}
}

{{ end -}}
